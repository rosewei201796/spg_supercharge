# Channel Cold Start Generator

一个使用 AI 为新频道冷启动生成多样化、风格统一的图片的最小化 Web 应用。

## 🎯 工作流程

1. 用户上传一张主题图片并输入文本提示词
2. 系统调用 **Gemini 3 Pro Preview** 和元提示词生成多个图像生成提示词
3. 将每个生成的提示词发送给 **Gemini 2.5 Flash Image** 模型生成图片
4. 在画廊中展示结果及其对应的提示词

## 🏗️ 技术栈

- **前端**: React 18 + TypeScript + Vite
- **后端**: Node.js + Express + TypeScript
- **AI API**: OpenAI-compatible endpoint (https://llm.jp.one2x.ai)
  - gemini/gemini-3-pro-preview (提示词生成)
  - gemini/gemini-2.5-flash-image (图像生成)
- **样式**: TailwindCSS
- **构建工具**: Vite, tsx, TypeScript

## 📁 项目结构

```
auto feed/
├── src/
│   ├── frontend/              # React 应用
│   │   ├── App.tsx            # 主 UI 组件
│   │   ├── main.tsx           # React 入口
│   │   ├── index.html         # HTML 模板
│   │   └── styles/
│   │       └── index.css      # TailwindCSS 样式
│   ├── api/                   # API 客户端模块
│   │   ├── gemini.ts          # Gemini 3 Pro Preview 客户端
│   │   └── nanoBanana.ts      # Gemini 2.5 Flash Image 客户端
│   ├── server.ts              # Express 服务器
│   └── systemPrompt.ts        # 冷启动提示词生成器系统提示
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

## 🚀 开始使用

### 前置要求

- Node.js 18+ 已安装
- npm 或 yarn 包管理器

### 安装

安装依赖：
```bash
npm install
```

### 开发

同时运行前端和后端服务器：

```bash
npm run dev
```

这将启动：
- **前端** (React + Vite): http://localhost:3000
- **后端** (Express): http://localhost:3001

前端会自动将 API 请求代理到后端。

### 单独运行服务器命令

```bash
# 仅运行后端
npm run dev:server

# 仅运行前端
npm run dev:client
```

### 生产构建

```bash
npm run build
npm run preview
```

## 🔌 API 集成

应用使用 **OpenAI SDK** 调用兼容 OpenAI 格式的 API 端点。

### API 配置

**文件**: `src/api/gemini.ts` 和 `src/api/nanoBanana.ts`

```typescript
const client = new OpenAI({
  apiKey: "sk-PZwjoX0QGseFOHjVh3SFBQ",
  baseURL: "https://llm.jp.one2x.ai",
});
```

### Gemini 3 Pro Preview (提示词生成)

**模型**: `gemini/gemini-3-pro-preview`

- ✅ 使用 OpenAI SDK
- ✅ 支持 vision（图片输入）
- ✅ 发送系统提示词 + 用户提示词 + 上传的图片
- ✅ 解析结构化 JSON 响应
- ✅ 返回 8-12 个图像生成提示词

### Gemini 2.5 Flash Image (图像生成)

**模型**: `gemini/gemini-2.5-flash-image`

- ✅ 使用 OpenAI SDK
- ✅ 发送文本提示词
- ✅ 从 `model_extra.images` 中提取 base64 图片
- ✅ 返回 data URL 格式的图片
- ✅ 包含错误处理和后备机制

## 📝 系统提示词

系统提示词定义在 `src/systemPrompt.ts` 中，它指示 Gemini 3 模型：

1. **提取核心字段**：从用户输入中提取主题、风格基线、变化因素
2. **生成权重**：为多样性生成权重（构图、光照、场景、情绪、纹理）
3. **创建 8-12 个多样化的图像提示词**：遵循特定模板
4. **保持视觉一致性**：在所有提示词中保持一致性的同时确保多样性

## 🎨 功能特性

- ✅ 简洁的 TailwindCSS UI
- ✅ 带预览的图片上传
- ✅ 多行提示词输入
- ✅ 加载状态和错误处理
- ✅ 响应式画廊网格
- ✅ 全栈 TypeScript
- ✅ 清晰的关注点分离
- ✅ OpenAI SDK 集成
- ✅ 生产就绪

## 🔧 自定义

### 修改 API 配置

如果需要更换 API 密钥或端点，编辑以下文件：
- `src/api/gemini.ts`
- `src/api/nanoBanana.ts`

```typescript
const client = new OpenAI({
  apiKey: "your-api-key-here",
  baseURL: "your-endpoint-here",
});
```

### 修改生成的提示词数量

编辑系统提示词中的建议范围（当前为 8-12 个）。Gemini 3 会根据系统提示词的指示生成。

### 样式定制

所有样式都在 `src/frontend/styles/index.css` 中使用 TailwindCSS 工具类。在那里自定义颜色、间距和组件。

## 📦 依赖项

**生产环境**:
- `openai` - OpenAI SDK（用于兼容 API）
- `express` - 后端服务器
- `cors` - CORS 中间件
- `react` + `react-dom` - 前端框架

**开发环境**:
- `typescript` - 类型安全
- `vite` - 快速构建工具
- `tsx` - TypeScript 执行
- `tailwindcss` - 工具优先的 CSS
- `concurrently` - 运行多个命令

## 🐛 故障排除

### 端口已被占用

在以下位置更改端口：
- 前端: `vite.config.ts` → `server.port`
- 后端: `src/server.ts` → `PORT` 常量

### 图片加载失败

检查浏览器控制台的 CORS 错误。后端默认启用了 CORS。

### API 认证错误

确保 API 密钥正确配置在 `src/api/` 文件中。

### 构建错误

确保所有依赖项已安装：
```bash
rm -rf node_modules package-lock.json
npm install
```

## 🚀 生产部署注意事项

1. **安全性**: API 密钥应该通过环境变量管理，不要硬编码
2. **配额**: 监控 API 的使用配额
3. **错误处理**: 当前图像生成失败会回退到占位符图片
4. **速率限制**: 考虑为高流量场景添加请求队列

## 📄 许可证

MIT

---

**状态**: ✅ 生产就绪，已集成 OpenAI-compatible API
